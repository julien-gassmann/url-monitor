meta {
  name: verify
  type: http
  seq: 1
}

get {
  url: {{base_url}}/tokens/verify/{{access_token}}
  body: none
  auth: none
}

script:post-response {
  if (res.body.is_valid) {
    bru.setEnvVar("bearer_token", res.body.access_token);
    bru.setEnvVar("monitor_uuid", res.body.monitor_uuid);
  }
}
