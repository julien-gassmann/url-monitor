meta {
  name: validate
  type: http
  seq: 3
}

post {
  url: {{base_url}}/monitors/validate
  body: json
  auth: none
}

body:json {
  {
    "url": "{{docker_base_url}}/metadata",
    "expected_http_code": 200,
    "frequency": "daily",
    "user_email": "test@test.com"
  }
}
