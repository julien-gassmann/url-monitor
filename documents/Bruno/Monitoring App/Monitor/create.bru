meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{base_url}}/monitors
  body: json
  auth: none
}

body:json {
  {
    "url": "{{docker_base_url}}/metadata",
    "expected_http_code": 200,
    "frequency": "daily",
    "user_email": "test@test.com"
  }
}

script:post-response {
  bru.setEnvVar("access_token", res.body.token);
}
