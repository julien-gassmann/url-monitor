meta {
  name: checks
  type: http
  seq: 5
}

get {
  url: {{base_url}}/monitors/{{monitor_uuid}}/checks?page=1&per_page=1
  body: none
  auth: bearer
}

params:query {
  page: 1
  per_page: 1
}

headers {
  : 
}

auth:bearer {
  token: {{bearer_token}}
}

body:json {
  {
    "url": "{{docker_base_url}}/metadata",
    "expected_http_code": 200,
    "frequency": "daily",
    "user_email": "test@test.com"
  }
}
