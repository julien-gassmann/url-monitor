{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "emails:build": "for f in resources/views/mails/*.mjml; do mjml \"$f\" -o \"${f%.mjml}.blade.php\"; done"
    }
}
